#!/usr/bin/env bash

# NOTE: Discord patch for niri/hyprland

# Check the dependency return true if installed dependency
function check_dep() {
    if [ $(uname) != "Linux" ]; then
        log_error "Cannot install discord on os except linux"
        return 1
    fi
    command -v vesktop 1>/dev/null 2>&1 && return 0
    return 1
}

# Things to do to install the dependency
function install_dep() {
    run paru -S vesktop-bin
}

# Things to do to install the item
function install() {
    run ln -sfn $rootdir/items/discord/src/vesktop.desktop $HOME/.local/share/applications/vesktop.desktop
    run mkdir -p $HOME/.config/vesktop/
    run ln -sfn $rootdir/items/discord/src/vesktop-flags.conf $HOME/.config/vesktop/vesktop-flags.conf
}

# Check if the item is installed, return true if installed
function check() {
    [[ -f $HOME/.local/share/applications/vesktop.desktop ]] && [[ -d $HOME/.config/vesktop ]] && return 0
    return 1
}

# Things to do to uninstall the item
function uninstall() {
    run rm $HOME/.local/share/applications/vesktop.desktop
    run rm -rf $HOME/.config/vesktop
}
