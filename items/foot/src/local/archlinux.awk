BEGIN {
    shell = "/usr/bin/zsh"
    font_family = "ComicCode Nerd Font"
    font_size = "9.5"
    notify_command = "notify-send -a ${app-id} -i ${app-id} ${title} ${body}command=notify-send -a ${app-id} -i ${app-id} ${title} ${body}command=notify-send -a ${app-id} -i ${app-id} ${title} ${body}command=notify-send -a ${app-id} -i ${app-id} ${title} ${body}"
    line_nr_max = 100000
}
